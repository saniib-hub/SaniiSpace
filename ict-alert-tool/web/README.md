# ICT Alert Tool — web frontend

React + TypeScript + Tailwind + shadcn/ui dashboard for the ICT backtest
engine. See `../README.md` for how to run this together with the API.

```bash
npm install
npm run dev
```
